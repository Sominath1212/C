//thise program has a incomplite
//program in process


#include<stdio.h>
#include<conio.h>
int main( )
{
    int a [ 5 ] , index = 0 , i;
    printf( "Enter any element for array=\n" );
    for ( i = 0; i < 5; i++ )
    {
        scanf( "%d" , &a [ i ] );
    }
    printf( "enter array index for you want to delete=\n" );
    scanf( "%d" , &index );
    printf( "\n*****result array*******\n" );
    for ( i = 0; i < 5; i++ )
    {
        if ( i == index )
        {
            /* code */
        }

    }



    return 0;
}